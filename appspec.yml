version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/trilo/ # 프로젝트 이름
    overwrite: yes # 덮어 씌움

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user